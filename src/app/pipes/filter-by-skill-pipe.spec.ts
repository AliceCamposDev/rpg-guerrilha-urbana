import { FilterBySkillPipe } from './filter-by-skill-pipe';

describe('FilterBySkillPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySkillPipe();
    expect(pipe).toBeTruthy();
  });
});
